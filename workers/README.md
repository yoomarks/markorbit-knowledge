# Workers

This directory is reserved for future Mo Crawl, Local Worker and converter runtimes.

KNOWLEDGE-TASK-001 intentionally adds no Python runtime, Crawl4AI dependency, scheduler, lease implementation or remote execution mechanism. Workers must later receive declarative, capability-scoped tasks only; arbitrary remote code execution is forbidden.
