# Fixtures

Repository-level fixtures and contract examples will live here. The first administration preview uses the clearly named module `apps/admin/src/fixtures/preview-data.ts`.

Fixture content must always be labeled as preview/demo data and must never be represented as a real acquisition result.
