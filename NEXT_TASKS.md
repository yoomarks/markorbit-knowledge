# MarkOrbit Knowledge — Next Tasks

**Canonical direction:** Web / AI / Expert / Case four-pillar Knowledge strategy  
**Checkpoint:** 2026-08-31  
**Verified Knowledge main before this docs branch:** `4968975fd20042f3c6fa65e5808fd0d618ba69e4`  
**Latest audited MarkOrbit/Core main:** `03d6260fdb33fdf697462af34433a9bdfb9cfd8e`

This is the short execution pointer. `docs/project/CURRENT_STATE.md` contains the fuller engineering checkpoint. Re-verify live GitHub state before protected, cross-repository or paid execution.

## Completed / frozen in the current stage

### Retrieval quality

The old post-#546 P0 is complete through real evidence-backed regression work:

- #604 — versioned representative retrieval regression corpus;
- #605 — metadata/filter vs lexical vs lexical+relationship comparator;
- #607 — real frozen Phase 2 USPTO live-corpus variant benchmark with exact document/chunk lineage;
- no blended relevance score and no synthetic vector/provider metrics.

Do not reopen “build a real frozen corpus/query runner” as a new task; it is already implemented.

### K-FED four-source retrieval

- #608 — WEB / AI / EXPERT / CASE federated read surface merged;
- WEB and AI reuse verified canonical retrieval and are classified from objective URI evidence (`http(s)` and `ai+markorbit:`);
- Expert remains native provenance-bearing Expert source evidence scoped by durable workspace task bindings;
- Case exposes promoted CaseCandidate descriptors, not dossier body content;
- results remain grouped by family with no cross-family blended score;
- #610 — repository-level acceptance proved all four families against real SQLite repositories and proved A/B workspace isolation.

### Case

- K-CASE cross-repository promotion/intake/collection acceptance is established;
- #603 refreshed the audited Core pin at its checkpoint;
- do not treat direct Case → Core ReadyPackage delivery as the K-CASE-008 terminal objective;
- do not chase later Core commits unless an actual drift/compatibility check requires a new pin.

### Evidence security

- #606 sanitizes retained bootstrap evidence before artifact upload so runner-local worker credential fields are null/redacted;
- never expose historical ephemeral runner-local credential values.

### Relationship / graph

- graph/relation repositories and read models exist;
- the real Phase 2 corpus currently has no trustworthy live relationship-edge evidence;
- relationship contribution/noise may therefore legitimately remain null;
- do not invent edges merely to produce positive graph lift.

## Active P0 gates

There is no separate unblocked internal P0 coding issue at this checkpoint. The active queue consists of real external/operator gates.

### P0-1 — Repository governance (#429)

The active `Protect main` ruleset already blocks deletion/non-fast-forward updates and requires strict `autoformat`, `validate (22)` and `validate (24)` checks plus review-thread resolution.

Current exact governance gap:

- `required_approving_review_count = 0`;
- `require_code_owner_review = false`.

Required action is a GitHub repository-admin change, not a code workaround. After #405 succeeds, durable non-public evidence archive closeout also remains required.

### P0-2 — Expert real live vertical slice (#468)

Knowledge-side contracts, persistence, retrieval and consumer seams exist, but do not assume the reusable production Shared Communication capability is complete.

Before execution:

- re-audit exact current Core/shared Communication capability;
- require a legitimate deployed sender/account;
- send one existing Expert task exactly once;
- preserve durable send/thread identity;
- import one real same-thread reply with immutable raw provenance and attachment lineage where present;
- prove replay creates no duplicate send or source record;
- keep delivery uncertainty fail closed.

Do not build a Knowledge-local generic email/provider transport.

### P0-3 — CNIPA authenticated live validation (#573)

Architecture/runtime readiness is implemented. Remaining work is operator-assisted live evidence:

- authorized human login/CAPTCHA completion;
- one real registration-number probe across the three candidate libraries;
- one real party-name/role-mapping probe;
- validate list → detail source identity;
- probe >100/page-11 behavior and record `PARTIAL`/`UNKNOWN` when exhaustive coverage cannot be proven;
- freeze endpoint/schema facts only after authenticated evidence.

Never automate CAPTCHA solving or bypass SSO/access controls.

### P0-4 — ADK-06 live 3×2 acceptance (#405)

Do not execute merely to keep development moving.

Run only when all gates are genuinely ready:

- explicit paid/live authorization;
- DeepSeek/OpenAI credentials;
- exact current Knowledge main SHA;
- protected execution/approval controls;
- evidence passphrase;
- authorized durable non-public archive destination.

Final acceptance remains 3 assignments × 2 real providers = 6 durable executed cells with exact RawArtifact lineage and no unresolved delivery uncertainty.

## Internal engineering trigger rule

Start a new Knowledge coding task only when at least one of these is true:

1. a live/production run exposes a reproducible acquisition, lineage, retrieval, isolation or interoperability defect;
2. a frozen regression corpus shows a measurable quality regression;
3. a cross-repository contract drift check proves compatibility work is required;
4. an explicit product requirement introduces a new evidence-supply capability that still belongs inside Knowledge.

If none is true, do not manufacture framework work.

## Do not start merely to keep coding

- another generic retrieval framework;
- another K-FED abstraction after #608/#610 without an observed defect;
- blended cross-channel relevance/truth scoring;
- synthetic vector/provider metrics;
- fabricated graph edges or relationship lift;
- Knowledge-local generic AI provider transport;
- Knowledge-local generic mailbox/provider platform;
- fake live Expert communication evidence;
- direct MarkReg DB/persistence reads;
- speculative Case ontology/framework expansion;
- broad Web extraction expansion without a concrete production gap;
- autonomous CNIPA auth/CAPTCHA bypass;
- paid/live #405 for roadmap optics.

## Operating rule

**抓大放小.** Prefer exact lineage, real source flows, measurable retrieval quality and durable cross-system evidence over additional framework surface.
