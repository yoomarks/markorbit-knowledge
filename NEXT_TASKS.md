# MarkOrbit Knowledge — Next Tasks

**Canonical direction:** Evidence Plane with governed consumer readiness
**Current convergence lane:** K0 owner/status freeze
**Checkpoint:** 2026-09-17

Re-verify live GitHub state before protected, cross-repository, authenticated-source, or paid execution.

## Current runtime reality

The earlier Phase 4 queue in this file is complete and is no longer the active execution order:

- `#725` Evidence Workspace — merged;
- `#726` Explainable Evidence Supply Health — merged;
- `#727` Evidence Sets / Review Packages — merged into the current evidence/runtime foundation.

K0 then froze the cross-system seams that future Knowledge work must preserve:

1. `#757` Core Workspace authority → durable Knowledge namespace binding — completed;
2. `#758` Evidence Plane vs Cognitive Plane owner boundary — completed;
3. `#759` one governed-current consumer model — completed;
4. `#760` executable retention, acquisition lifecycle and platform operations — completed;
5. `#761` health/readiness taxonomy and owner documentation — current final convergence pass.

Do not reopen the old Phase 4 sequence as if `#726/#727` were pending.

## K0 exit invariants

Future work must extend, not fork, these seams:

- Core Workspace UUID is the authenticated tenant authority; Knowledge `wsp_*` is private persistence namespace only.
- Knowledge owns objective evidence acquisition, provenance, versions, evidence-side operational projections and consumer admissibility.
- Brain/Method owns interpretation, resolution, synthesis, distillation and reasoning-method meaning.
- Capability owns governed implementation binding, invocation and protected execution semantics.
- `CURRENT != VERIFIED != CONSUMER_ADMISSIBLE != DELIVERED`.
- Health/readiness projections follow `docs/architecture/K0_HEALTH_READINESS_TAXONOMY_AND_OWNER_MAP.md`; no universal Knowledge health state exists.
- Platform Operations Portfolio aggregates facts but does not become semantic or consumer-readiness authority.
- Main-repo `services/knowledge` is an integration bridge, not a second Knowledge domain owner.

## What may start after K0

Start a Knowledge task only when it has concrete acceptance criteria and fits at least one of these classes:

1. a reproducible evidence-integrity, acquisition, lineage, retrieval, isolation or interoperability defect;
2. a frozen regression corpus exposes measurable correctness loss;
3. a cross-repository contract drift check proves compatibility work is required;
4. an accepted requirement introduces an evidence-plane capability that clearly belongs in Knowledge;
5. an observed operator-throughput, coverage-truth, review-usability or evidence-inspection gap blocks effective use of durable evidence.

New work must name the existing owner/readiness seam it extends before implementation begins.

## External/operator gates that remain real

### ADK-06 live 3×2 acceptance (#405)

The paid/live provider acceptance remains an explicit operator gate. Run only with fresh owner authorization, the exact current Knowledge main SHA, protected execution controls, real provider credentials and authorized durable non-public evidence retention. K0 convergence does not authorize or substitute for that live run.

### Repository governance / live evidence retention (#429)

Repository governance remains separate from product work. Preserve protected main, workflow review, protected live credentials, exact-SHA execution and durable non-public live-evidence retention requirements. Do not weaken these controls to simplify development.

### CNIPA backend-only evidence debt (#691)

The bounded CNIPA MVP remains complete while backend-only source identity, schema, pagination and population-coverage facts stay explicitly unverified. Advance `#691` only when a legitimate permitted authenticated raw/source-response channel exists. Do not bypass CAPTCHA/SSO, anti-debug protections, or extract/replay session credentials.

## Do not start merely to keep coding

- another generic retrieval/search framework;
- another health/readiness state machine for facts already covered by the K0 taxonomy;
- blended cross-channel relevance/truth scoring;
- synthetic vector/provider metrics;
- fabricated graph edges or graph lift;
- Knowledge-local generic AI provider transport;
- Knowledge-local generic mailbox/provider platform;
- a second Brain/Method interpretation layer inside Knowledge;
- a second Core Workspace or product-lifecycle authority;
- fake live Expert communication evidence;
- direct MarkReg DB/persistence reads;
- speculative Case ontology/framework expansion;
- broad Web extraction expansion without a concrete production gap;
- autonomous CNIPA auth/CAPTCHA bypass;
- paid/live `#405` for roadmap optics;
- a second Evidence Inspector, review-package runtime or delivery engine when durable contracts can be reused.

## Operating rule

**抓大放小.** Prefer exact ownership, complete corpus truth, durable lineage, explainable status, factual supply health, reproducible evidence contexts and governed cross-system handoff over additional framework surface.

When a UI needs a status, select the narrowest canonical projection for that decision instead of inventing a composite score. When a downstream system needs meaning, hand off admissible evidence to the owning Cognitive/Capability plane instead of expanding Knowledge authority.
