import { describe, expect, it } from "vitest";

describe("scheduled collection dispatcher", () => {
  it("dispatches a scheduled collection job into the execution chain", async () => {
    expect(true).toBe(true);
  });
});
