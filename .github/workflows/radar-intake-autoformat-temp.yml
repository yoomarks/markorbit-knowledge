name: Radar Intake Autoformat Temp

on:
  push:
    branches: [agent/radar-source-intake-v1]

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/radar-source-intake-v1
      - uses: pnpm/action-setup@v4
        with:
          version: 11.13.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Format Radar intake files
        run: |
          pnpm exec prettier --write apps/worker/src/radar-source-intake.ts apps/worker/tests/radar-source-intake.test.ts
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/worker/src/radar-source-intake.ts apps/worker/tests/radar-source-intake.test.ts
          git commit -m "style: format radar intake sources"
          git push origin HEAD:agent/radar-source-intake-v1
