name: Agent Source Intelligence Evidence Calibration Live

on:
  push:
    branches:
      - agent/source-intelligence-before-after-evidence
    paths:
      - .github/workflows/agent-source-intelligence-evidence-calibration-live.yml

permissions:
  contents: read

jobs:
  live-proof:
    uses: ./.github/workflows/source-intelligence-evidence-calibration.yml
    with:
      keys: "uspto-trademarks,finnegan,inta"
      min_success: "2"
