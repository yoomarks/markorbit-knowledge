name: Temporary scheduler due fairness verification

on:
  push:
    branches:
      - codex/scheduler-due-fairness-v1

permissions:
  contents: write

jobs:
  verify:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python .github/tmp/patch_scheduler_due_fairness.py
      - uses: pnpm/action-setup@v4
        with:
          version: 11.13.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec prettier --write packages/persistence/src/collection-scheduler.ts packages/persistence/tests/collection-scheduler.test.ts
      - run: pnpm --filter @markorbit/persistence exec vitest run tests/collection-scheduler.test.ts
      - run: pnpm typecheck
      - name: Commit integration
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add packages/persistence/src/collection-scheduler.ts packages/persistence/tests/collection-scheduler.test.ts
          git commit -m "Scheduler: prioritize due plans under bounded ticks" || true
          git push
