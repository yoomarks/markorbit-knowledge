name: GitHub Connector Hardening Once

on:
  push:
    branches:
      - codex/connector-github-v1

permissions:
  contents: write

jobs:
  harden:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/connector-github-v1
      - uses: pnpm/action-setup@v4
        with:
          version: 11.13.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Apply connector hardening
        run: python .github/scripts/github-connector-hardening.py
      - name: Format permanent files
        run: >-
          pnpm exec prettier --write
          README.md
          apps/worker/package.json
          apps/worker/src/bootstrap-github.ts
          apps/worker/src/config.ts
          apps/worker/src/main.ts
          apps/worker/tests/config.test.ts
          docs/operations/GITHUB_CONNECTOR_V1.md
          packages/worker-runtime/src/github-acquirer.ts
          packages/worker-runtime/src/index.ts
          packages/worker-runtime/tests/github-acquirer.test.ts
      - name: Run focused connector checks
        run: |
          pnpm --filter @markorbit/worker-runtime test -- tests/api-acquirer.test.ts tests/rss-acquirer.test.ts tests/github-acquirer.test.ts
          pnpm --filter @markorbit/worker test -- tests/config.test.ts
          pnpm --filter @markorbit/worker-runtime typecheck
          pnpm --filter @markorbit/worker typecheck
          pnpm format:check
      - name: Commit hardened implementation
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add .env.example README.md apps/worker docs/operations/GITHUB_CONNECTOR_V1.md packages/worker-runtime/src packages/worker-runtime/tests/github-acquirer.test.ts
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m 'fix(github): harden repository snapshot boundaries'
          git push origin HEAD:codex/connector-github-v1
