# Change Evidence V1 lineage

`Source → CollectionRun → RawArtifact → Conversion → Staging → RetrievalDocument → DocumentChangeEvent → DocumentChangeEvidence`

Change Evidence does not replace any upstream object. It projects objective before/after facts from persisted immutable lineage so downstream systems can reason over changes without losing provenance.
